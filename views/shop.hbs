        <main>
        {{#if hasProducts}}
        <div class="grid">
            {{#each products}}
            <article class="card product-item">
                <header class="card__header">
                    <h1 class="product__title">{{productName}}</h1>
                </header>
                <div class="card__image">
                    <img src="https://source.unsplash.com/random/400x400" alt="Random Placeholder Image Provided by Unsplash">
                </div>
                <div class="card__content">
                    <h2 class="product__price">${{productPrice}}</h2>
                    <p class="product__description">{{productDescription}}</p>
                </div>
                <div class="card__actions">
                    <button class="btn">Add to Cart</button>
                </div>
            </article>
            {{/each}}
            {{else}}
            <h1>No Products.</h1>
            <p>Create new products by clicking "Add Products" in the navbar.</p>
            {{/if}}
        </div>
    </main>